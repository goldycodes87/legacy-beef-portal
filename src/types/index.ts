/**
 * Retired. These interfaces described the Stripe-era schema (they still
 * referenced stripe_payment_intent_id and an 'echeck' method) and were not
 * imported anywhere. Types now live next to the code that uses them.
 *
 * Safe to delete this file.
 */
export {};
